// Ternary Operators Conditional Operator

// Syntax ?: i.e. condition ? condition true result : condition false result

// Ex1
let a = 100, b= 200;

let result = (a > b) ? a : b
console.log(result)

// Ex1

let age = 30;

let canVote = age > 18? "Person Can Vote" :  "Person Cannot Vote"
console.log(canVote)